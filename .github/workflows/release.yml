-name: Build & Release
-
-on:
-  push:
-    tags:
-      - 'v*'
+name: Build & Release
+
+on:
+  release:
+    types: [published]

 jobs:
   build_and_release:
     runs-on: windows-latest
     steps:
       - uses: actions/checkout@v3
       # … (ขั้นตอนอื่น ๆ เหมือนเดิม) …
